Place your custom font file here.

1. Add your `.ttf` or `.otf` file to this folder.
2. Rename it to `FlounderBrand.ttf` (or update `hooks/useAppFonts.ts` to match your filename).
3. Reload the app.

The logo text in `components/home/HomeHeader.tsx` uses `fonts.brand` from `constants/fonts.ts`.
